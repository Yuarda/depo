<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yarda.wordgame">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Deneme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".AddWordActivity"
            android:exported="false" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".AnalysisActivity"
            android:exported="false" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".WordleActivity"
            android:exported="false" /> <!-- Theme kısmı sende farklı olabilir, dokunma -->
        <!-- 3. ANA EKRAN (Uygulamanın başlayacağı yer) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" /> <!-- BU SATIR ÇOK ÖNEMLİ -->
            </intent-filter>
        </activity> <!-- 4. DİĞER EKRANLAR (Quiz Ekranı) -->
        <activity
            android:name=".QuizActivity"
            android:exported="false" />
    </application>

</manifest>